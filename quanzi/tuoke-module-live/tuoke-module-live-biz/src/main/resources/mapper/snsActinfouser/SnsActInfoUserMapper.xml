<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.metast.tuoke.module.live.dal.mysql.snsActinfouser.SnsActInfoUserMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <resultMap type="cn.metast.tuoke.module.live.controller.admin.snsActinfouser.vo.SnsActInfoUserRespVO" id="SnsActInfoUserResult">
        <result property="id"    column="id"    />
        <result property="actId"    column="act_id"    />
        <result property="userId"    column="user_id"    />
        <result property="fieldName"    column="field_name"    />
        <result property="fieldValue"    column="field_value"    />
        <result property="actUserId"    column="act_user_id"    />
    </resultMap>
    <sql id="selectSnsActInfoUserVo">
        select id, act_id, user_id, field_name, field_value,act_user_id from sns_act_info_user
    </sql>
    <!--批量插入报名信息-->
    <select id="selectSnsActInfoUserById" parameterType="Long" resultMap="SnsActInfoUserResult">
        <include refid="selectSnsActInfoUserVo"/>
        where id = #{id}
    </select>
    <insert id="insertSnsActInfoUserList" parameterType="java.util.List"
            useGeneratedKeys="true" keyProperty="id">
        insert into sns_act_info_user (act_id,user_id,field_name,field_value,act_user_id)
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.actId}, #{item.userId},#{item.fieldName},#{item.fieldValue},#{item.actUserId})
        </foreach>
    </insert>
    <select id="selectSnsActInfoUserList" parameterType="cn.metast.tuoke.module.live.controller.admin.snsActinfouser.vo.SnsActInfoUserPageReqVO" resultMap="SnsActInfoUserResult">
        <include refid="selectSnsActInfoUserVo"/>
        <where>
            <if test="reqvo.actId != null "> and act_id = #{reqvo.actId}</if>
            <if test="reqvo.userId != null "> and user_id = #{reqvo.userId}</if>
            <if test="reqvo.actUserId != null "> and act_user_id = #{reqvo.actUserId}</if>
            <if test="reqvo.fieldName != null  and reqvo.fieldName != ''"> and field_name = #{reqvo.fieldName}</if>
            <if test="reqvo.fieldValue != null  and reqvo.fieldValue != ''"> and field_value = #{reqvo.fieldValue}</if>
        </where>
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.metast.tuoke.module.live.dal.mysql.snsComment.SnsCommentMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <resultMap type="cn.metast.tuoke.module.live.dal.dataobject.snsComment.SnsCommentDO" id="SnsCommentResult">
        <result property="commentId"    column="comment_id"    />
        <result property="postId"    column="post_id"    />
        <result property="userId"    column="user_id"    />
        <result property="userName"    column="userName"    />
        <result property="content"    column="content"    />
        <result property="likeNum"    column="like_num"    />
        <result property="tail"    column="tail"    />
        <result property="createTime"    column="create_time"    />
    </resultMap>
    <sql id="selectSnsCommentVo">
        select c.*, u.nickname userName
        from sns_comment c
                 left join system_users u on c.user_id = u.id
    </sql>
    <select id="selectSnsCommentList" parameterType="cn.metast.tuoke.module.live.dal.dataobject.snsComment.SnsCommentDO" resultMap="SnsCommentResult">
        <include refid="selectSnsCommentVo"/>
        <where>
            <if test="postId != null "> and c.post_id = #{postId}</if>
            <if test="userId != null "> and c.user_id = #{userId}</if>
            <if test="content != null  and content != ''"> and c.content = #{content}</if>
            <if test="likeNum != null "> and c.like_num = #{likeNum}</if>
            <if test="tail != null  and tail != ''"> and c.tail = #{tail}</if>
            <if test="type != null "> and c.type = #{type}</if>
        </where>
        order by create_time ASC
    </select>
    <delete id="deleteSnsCommentByPostId" parameterType="Long">
        delete from sns_comment where post_id = #{postId}
    </delete>
</mapper>

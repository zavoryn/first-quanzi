<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.metast.tuoke.module.live.dal.mysql.snsAblum.SnsAblumMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <resultMap type="cn.metast.tuoke.module.live.dal.dataobject.snsAblum.SnsAblumDO" id="SnsAblumResult">
        <result property="id"    column="id"    />
        <result property="postId"    column="post_id"    />
        <result property="url"    column="url"    />
        <result property="identifier"    column="identifier"    />
        <result property="fileSize"    column="file_size"    />

        <result property="type"    column="type"    />
        <result property="userId"    column="user_id"    />
        <result property="userName"    column="user_name"    />
        <result property="nickName"    column="nick_name"    />
    </resultMap>
    <sql id="selectSnsAblumVo">
        select id, post_id, url,identifier,type,user_id,user_name from sns_ablum
    </sql>
    <delete id="deleteSnsAblumById" parameterType="Long">
        delete from sns_ablum where post_id = #{id}
    </delete>
    <delete id="deleteSnsAblumByPostId" parameterType="Long">
        delete from sns_ablum where post_id = #{postId}
    </delete>
    <select id="selectSnsAblumList" parameterType="cn.metast.tuoke.module.live.dal.dataobject.snsAblum.SnsAblumDO" resultMap="SnsAblumResult">
        select t1.id, t1.post_id, t1.url,t1.identifier,t1.type,t1.user_id,t1.user_name
        from sns_ablum t1
        where t1.type is NULL
        <if test="postId != null "> and t1.post_id = #{postId}</if>
        <if test="url != null  and url != ''"> and t1.url = #{url}</if>
    </select>
    <select id="selectSnsAblumPostId" parameterType="Long" resultMap="SnsAblumResult">
        <include refid="selectSnsAblumVo"/>
        where post_id = #{postId}
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.metast.tuoke.module.heal.dal.mysql.detection.DetectionMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <resultMap type="cn.metast.tuoke.module.heal.dal.dataobject.detection.DetectionDO" id="DetectionDOResult">
        <result property="id"    column="id"    />
        <result property="deviceSn"    column="device_sn"    />
        <result property="uid"    column="uid"    />
        <result property="aid"    column="aid"    />
        <result property="name"    column="name"    />
        <result property="report"    column="report"    />
        <result property="unit"    column="unit"    />
        <result property="type"    column="type"    />
        <result property="ckrange"    column="ckrange"    />
        <result property="status"    column="status"    />
        <result property="iswc"    column="iswc"    />
        <result property="deleted"    column="deleted"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <select id="getExcelList" parameterType="cn.metast.tuoke.module.heal.controller.admin.detection.vo.DetectionRespVO" resultMap="DetectionDOResult">
        select id, device_sn, uid, aid, `name`, report, unit, `type`, ckrange, status, iswc, deleted, create_time, update_time
        from wn_detection
        <where>
            and deleted = '0'
            <if test="deviceSn != null  and deviceSn != ''"> and device_sn = #{deviceSn}</if>
            <if test="uid != null "> and uid = #{uid}</if>
            <if test="aid != null "> and aid = #{aid}</if>
            <if test="name != null  and name != ''"> and `name` like concat('%', #{name}, '%')</if>
            <if test="iswc != null  and iswc != ''"> and iswc = #{iswc}</if>
            <if test="ids != null ">
                and id in
                <foreach item="id" collection="ids" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
        </where>
    </select>


    <select id="selectByUid" parameterType="cn.metast.tuoke.module.heal.controller.admin.detection.vo.DetectionRespVO" resultMap="DetectionDOResult">
        select id, device_sn, uid, aid, `name`, report, unit, `type`, ckrange, status, iswc, deleted, create_time, update_time
        from wn_detection where deleted = '0' ORDER BY create_time DESC limit 1
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.metast.tuoke.module.heal.dal.mysql.healKnowledge.HealKnowledgeMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.metast.cn/MyBatis/x-plugins/
     -->
    <select id="getCourseList" resultType="cn.metast.tuoke.module.heal.controller.app.vo.HealCourseDo">
        SELECT 'ol' as 'type', id, `name` as 'name', logo as 'logoUrl', type_id as 'typeId', type_name as 'typeName', '' as 'content', introduce as 'introduce',
        visit_num as 'visitNum', watch_num as 'watchNum', comment_num as 'commentNum', favorite_num as 'favoriteMum', like_num as 'likeNum',
        create_time as 'createTime'
        FROM `education`.edu_course_ol where `status` = 1
        <if test="reqvo.codes != null">
            and type_id in (
            select type_id from `education`.edu_type
            where code in
            <foreach item="code" collection="reqvo.codes" open="(" separator="," close=")">
                #{code}
            </foreach>
            )
        </if>

        UNION ALL

        SELECT 'study' as 'type', id, `task_name` as 'name', logo_url as 'logoUrl', type_id as 'typeId', type_name as 'typeName', content as 'content', '' as 'introduce',
        visit_num as 'visitNum', watch_num as 'watchNum', comment_num as 'commentNum', favorite_num as 'favoriteMum', like_num as 'likeNum',
        create_time as 'createTime'
        FROM `education`.edu_course_study where `status` = 1
        <if test="reqvo.codes != null">
            and type_id in (
            select type_id from `education`.edu_type
            where code in
            <foreach item="code" collection="reqvo.codes" open="(" separator="," close=")">
                #{code}
            </foreach>
            )
        </if>

        UNION ALL

        SELECT 'tx' as 'type', id, `name` as 'name', logo as 'logoUrl', type_id as 'typeId', type_name as 'typeName', content as 'content', introduce as 'introduce',
        visit_num as 'visitNum', watch_num as 'watchNum', comment_num as 'commentNum', favorite_num as 'favoriteMum', like_num as 'likeNum',
        create_time as 'createTime'
        FROM `education`.edu_course_tx where `status` = 1
        <if test="reqvo.codes != null">
            and type_id in (
            select type_id from `education`.edu_type
            where code in
            <foreach item="code" collection="reqvo.codes" open="(" separator="," close=")">
                #{code}
            </foreach>
            )
        </if>

        ORDER BY createTime desc
    </select>
</mapper>
